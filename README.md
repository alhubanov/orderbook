# orderbook
A hash table implementation in Rust + an orderbook visualization

First idea is to implement a simple hashing function based on the first thing I can think of - summing the ASCII values of the characters in the string.
